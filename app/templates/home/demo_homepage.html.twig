{# templates/home/demo_homepage.html.twig #}








<!DOCTYPE html>
<html>
<head>
  <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0/dist/css/select2.min.css" rel="stylesheet" />
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0/dist/js/select2.min.js"></script>
</head>
<body style="background:#222; color:#eee; padding: 20px;">

  <label for="songs">Songs:</label>
  <select id="songs" multiple style="width: 300px;">
    <option>Safe From Harm - 2012 Mix/Master</option>
    <option>Peri Afovias O Logos</option>
    <option>Afou Sou'to Pa</option>
  </select>

  <script>
    $(document).ready(function() {
      $('#songs').select2({
        placeholder: "Select songs..."
      });
    });
  </script>
</body>
</html>


{#{% extends 'base.html.twig' %}

{% block title %}Bandito Demo Homepage Styles{% endblock %}

{% block body %}
{% set cards = [
  { title: 'Bands', subtitle: 'Explore all bands', path: path('band_list'), icon: '<svg class="w-16 h-16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><circle cx="12" cy="12" r="10" /><path stroke-linecap="round" stroke-linejoin="round" d="M8 12h8M12 8v8" /></svg>' },
  { title: 'Musicians', subtitle: 'Discover musicians', path: path('band_list'), icon: '<svg class="w-16 h-16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><rect x="3" y="4" width="18" height="16" rx="2" ry="2" /><path stroke-linecap="round" stroke-linejoin="round" d="M3 10h18" /></svg>' },
  { title: 'Albums', subtitle: 'Browse albums', path: path('band_list'), icon: '<svg class="w-16 h-16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><circle cx="12" cy="12" r="8" /><circle cx="12" cy="12" r="3" /></svg>' },
  { title: 'Songs', subtitle: 'Listen to songs', path: path('band_list'), icon: '<svg class="w-16 h-16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" d="M9 19V6l12-2v13" /><circle cx="6" cy="18" r="3" /></svg>' }
] %}


<div class="flex flex-col items-center justify-center min-h-[calc(100vh-64px)] space-y-6">
  <div class="w-full max-w-7xl">

    <h1 class="text-5xl md:text-5xl font-bold mb-12 tracking-tight text-yellow-600 text-center">
      Welcome to <span class="text-yellow-500">Bandito</span>
    </h1>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-12 w-full">

        {% for card in cards %}
        <a href="{{ card.path }}"
            class="flex flex-col items-center rounded-xl bg-yellow-50 p-10 shadow-md hover:shadow-lg transition-shadow duration-300 transform hover:-translate-y-1">
            <div class="mb-5 text-yellow-400" aria-hidden="true">{{ card.icon|raw }}</div>
            <h2 class="text-2xl font-semibold mb-2">{{ card.title }}</h2>
            <p class="text-yellow-600 text-center">{{ card.subtitle }}</p>
        </a>
        {% endfor %}

    </div>

  </div>
</div>

<div class="flex flex-col items-center justify-center min-h-[calc(100vh-64px)] space-y-6">
  <div class="w-full max-w-7xl bg-pink-200/20 dark:bg-indigo-200/10 backdrop-blur-xl border-2 border-yellow-500/60 rounded-3xl shadow-2xl px-10 py-14">

    <h1 class="text-5xl md:text-5xl font-bold mb-12 tracking-tight text-yellow-600 text-center">
      Welcome to <span class="text-yellow-500">Bandito</span>
    </h1>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-12 w-full">

        {% for card in cards %}
        <a href="{{ card.path }}"
            class="flex flex-col items-center rounded-xl bg-yellow-50 p-10 shadow-md hover:shadow-lg transition-shadow duration-300 transform hover:-translate-y-1">
            <div class="mb-5 text-yellow-400" aria-hidden="true">{{ card.icon|raw }}</div>
            <h2 class="text-2xl font-semibold mb-2">{{ card.title }}</h2>
            <p class="text-yellow-600 text-center">{{ card.subtitle }}</p>
        </a>
        {% endfor %}

    </div>

  </div>
</div>

<div
    x-data="{ show: false }"
    x-init="setTimeout(() => show = true, 100)"
    x-show="show"
    x-transition
    class="min-h-screen flex items-center justify-center px-4 bg-gradient-to-b from-yellow-100 via-white to-sky-100 dark:from-black dark:via-zinc-900 dark:to-zinc-800"
>
    <div class="max-w-6xl w-full p-8 rounded-2xl border border-white/10 dark:border-white/5 bg-white/30 dark:bg-black/30 backdrop-blur-2xl shadow-[0_8px_30px_rgba(0,0,0,0.1)] dark:shadow-[0_8px_30px_rgba(255,215,0,0.1)] transition duration-500 ease-in-out">
        <h1 class="text-3xl md:text-4xl font-bold text-center text-yellow-600 dark:text-yellow-400 font-display mb-10">
            Welcome to Bandito
        </h1>

        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-6">
            <!-- Card -->
            <a href="#" class="group p-6 rounded-xl bg-gradient-to-br from-yellow-50 to-yellow-100 dark:from-yellow-900 dark:to-yellow-800 shadow-md hover:ring-2 hover:ring-yellow-400/50 transition">
                <div class="flex flex-col items-center text-center space-y-3">
                    <div class="text-4xl text-yellow-500 group-hover:scale-110 transition">
                        ➕
                    </div>
                    <h2 class="text-lg font-semibold text-gray-800 dark:text-gray-100">Bands</h2>
                    <p class="text-sm text-orange-600 dark:text-orange-300">Explore all bands</p>
                </div>
            </a>

            <!-- Repeat for other cards -->
            <a href="#" class="group p-6 rounded-xl bg-gradient-to-br from-yellow-50 to-yellow-100 dark:from-yellow-900 dark:to-yellow-800 shadow-md hover:ring-2 hover:ring-yellow-400/50 transition">
                <div class="flex flex-col items-center text-center space-y-3">
                    <div class="text-4xl text-yellow-500 group-hover:scale-110 transition">
                        🧑‍🎤
                    </div>
                    <h2 class="text-lg font-semibold text-gray-800 dark:text-gray-100">Musicians</h2>
                    <p class="text-sm text-orange-600 dark:text-orange-300">Discover musicians</p>
                </div>
            </a>

            <a href="#" class="group p-6 rounded-xl bg-gradient-to-br from-yellow-50 to-yellow-100 dark:from-yellow-900 dark:to-yellow-800 shadow-md hover:ring-2 hover:ring-yellow-400/50 transition">
                <div class="flex flex-col items-center text-center space-y-3">
                    <div class="text-4xl text-yellow-500 group-hover:scale-110 transition">
                        💿
                    </div>
                    <h2 class="text-lg font-semibold text-gray-800 dark:text-gray-100">Albums</h2>
                    <p class="text-sm text-orange-600 dark:text-orange-300">Browse albums</p>
                </div>
            </a>

            <a href="#" class="group p-6 rounded-xl bg-gradient-to-br from-yellow-50 to-yellow-100 dark:from-yellow-900 dark:to-yellow-800 shadow-md hover:ring-2 hover:ring-yellow-400/50 transition">
                <div class="flex flex-col items-center text-center space-y-3">
                    <div class="text-4xl text-yellow-500 group-hover:scale-110 transition">
                        🎵
                    </div>
                    <h2 class="text-lg font-semibold text-gray-800 dark:text-gray-100">Songs</h2>
                    <p class="text-sm text-orange-600 dark:text-orange-300">Listen to songs</p>
                </div>
            </a>
        </div>
    </div>
</div>
  <!-- Glassy pastel wrapper (smaller and centered) -->

  <!-- Glassy pastel wrapper: centered, smaller, gold border -->
<div class="flex flex-col items-center justify-center min-h-[calc(100vh-64px)] space-y-6">
  <div class="w-full max-w-4xl bg-pink-200/20 dark:bg-indigo-200/10 backdrop-blur-xl border-2 border-yellow-500/60 rounded-3xl shadow-2xl px-10 py-14">

    <h1 class="text-4xl md:text-5xl font-bold mb-12 tracking-tight text-yellow-600 text-center">
      Welcome to <span class="text-yellow-500">Bandito</span>
    </h1>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-8">
      {% for card in cards %}
        <a href="{{ card.path }}"
           class="bg-white p-6 rounded-xl shadow-md hover:shadow-lg transition duration-300 transform hover:-translate-y-1 hover:scale-105 text-center dark:bg-zinc-800">
          <div class="mb-4 text-yellow-400" aria-hidden="true">
            {{ card.icon|raw }}
          </div>
          <h2 class="text-lg font-semibold">{{ card.title }}</h2>
          <p class="text-sm text-yellow-600 dark:text-yellow-400 mt-1">{{ card.subtitle }}</p>
        </a>
      {% endfor %}
    </div>

  </div>
</div>

<section class="min-h-screen flex items-center justify-center px-6 py-20 font-sans relative overflow-hidden">

  <!-- Glass wrapper -->
  <div class="absolute inset-0 bg-white/40 dark:bg-white/10 backdrop-blur-md z-0"></div>

  <!-- Main content -->
  <div class="relative z-10 text-center max-w-7xl w-full">
    <h1 class="text-5xl font-bold mb-16 tracking-tight text-yellow-600">Welcome to <span class="text-yellow-500">Bandito</span></h1>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-10">
        {% for card in cards %}
            <a href="{{ card.path }}" class="bg-white p-8 rounded-2xl shadow-xl hover:shadow-2xl transition duration-300 transform hover:-translate-y-1 hover:scale-105 text-center dark:bg-zinc-800">
                <div class="mb-6 text-yellow-400" aria-hidden="true">{{ card.icon|raw }}</div>
                <h2 class="text-xl font-semibold">{{ card.title }}</h2>
                <p class="text-sm text-yellow-600 dark:text-yellow-400 mt-1">{{ card.subtitle }}</p>
            </a>
        {% endfor %}
    </div>
  </div>
</section>

<section class="min-h-screen flex items-center justify-center px-6 py-20 font-sans relative overflow-hidden">

  <!-- Glassy pastel wrapper -->
  <div class="absolute inset-0 bg-pink-200/30 dark:bg-indigo-200/10 backdrop-blur-xl rounded-none z-0"></div>

  <!-- Main content -->
  <div class="relative z-10 text-center max-w-7xl w-full">
    <h1 class="text-5xl font-bold mb-16 tracking-tight text-yellow-600">Welcome to <span class="text-yellow-500">Bandito</span></h1>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-10">
        {% for card in cards %}
            <a href="{{ card.path }}" class="bg-white p-8 rounded-2xl shadow-xl hover:shadow-2xl transition duration-300 transform hover:-translate-y-1 hover:scale-105 text-center dark:bg-zinc-800">
                <div class="mb-6 text-yellow-400" aria-hidden="true">{{ card.icon|raw }}</div>
                <h2 class="text-xl font-semibold">{{ card.title }}</h2>
                <p class="text-sm text-yellow-600 dark:text-yellow-400 mt-1">{{ card.subtitle }}</p>
            </a>
        {% endfor %}
    </div>
  </div>
</section>

<section class="min-h-screen bg-gradient-to-b from-yellow-50 to-white flex flex-col items-center justify-center px-6 py-20 font-sans text-gray-800">
  <h1 class="text-5xl font-bold mb-16 tracking-tight text-yellow-600">Welcome to <span class="text-yellow-500">Bandito</span></h1>
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-10 max-w-7xl w-full">

    {% for card in cards %}
      <a href="{{ card.path }}"
         class="bg-white p-8 rounded-2xl shadow-xl hover:shadow-2xl transition duration-300 transform hover:-translate-y-1 hover:scale-105 text-center">
        <div class="mb-6 text-yellow-400" aria-hidden="true">{{ card.icon|raw }}</div>
        <h2 class="text-xl font-semibold">{{ card.title }}</h2>
        <p class="text-sm text-yellow-600 mt-1">{{ card.subtitle }}</p>
      </a>
    {% endfor %}

  </div>
</section>

{% set pastelColors = ['bg-pink-100 text-pink-800', 'bg-green-100 text-green-800', 'bg-yellow-100 text-yellow-800', 'bg-blue-100 text-blue-800'] %}

<section class="min-h-screen bg-white flex flex-col items-center justify-center px-6 py-20 font-sans">
  <h1 class="text-5xl font-bold mb-16 text-gray-800">Welcome to <span class="text-yellow-500">Bandito</span></h1>
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-10 max-w-7xl w-full">

    {% for card in cards %}
      {% set style = pastelColors[loop.index0 % pastelColors|length] %}
      <a href="{{ card.path }}"
         class="rounded-2xl p-10 shadow-md hover:shadow-lg transition duration-300 transform hover:-translate-y-1 text-center {{ style }}">
        <div class="mb-4" aria-hidden="true">{{ card.icon|raw }}</div>
        <h2 class="text-xl font-bold">{{ card.title }}</h2>
        <p class="text-sm mt-1">{{ card.subtitle }}</p>
      </a>
    {% endfor %}

  </div>
</section>

<section class="min-h-screen bg-gray-100 px-6 py-20 font-sans text-gray-800">
  <h1 class="text-4xl sm:text-5xl font-bold text-center mb-16 text-yellow-500">Welcome to Bandito</h1>
  <div class="columns-1 sm:columns-2 md:columns-4 gap-6 space-y-6 max-w-7xl mx-auto">

    {% for card in cards %}
      <a href="{{ card.path }}"
         class="block bg-white rounded-xl p-8 shadow-sm hover:shadow-md break-inside-avoid-column transition hover:-translate-y-1 duration-300">
        <div class="mb-4 text-yellow-400">{{ card.icon|raw }}</div>
        <h2 class="text-xl font-semibold">{{ card.title }}</h2>
        <p class="text-yellow-700 text-sm mt-1">{{ card.subtitle }}</p>
      </a>
    {% endfor %}

  </div>
</section>

<section class="min-h-screen bg-gradient-to-b from-yellow-50 to-white flex flex-col items-center justify-center px-6 py-20 font-sans text-gray-800">
  <h1 class="text-5xl font-bold mb-16 tracking-tight text-yellow-600">
    Welcome to <span class="text-yellow-500">Bandito</span>
  </h1>

  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-10 max-w-7xl w-full">

    {% for card in cards %}
      <a href="{{ card.path }}"
         class="bg-white/10 backdrop-blur-lg border border-white/20 text-center p-8 rounded-2xl shadow-xl hover:shadow-2xl transition duration-300 transform hover:-translate-y-1 hover:scale-105">
        <div class="mb-6 text-yellow-300 text-4xl" aria-hidden="true">{{ card.icon|raw }}</div>
        <h2 class="text-xl font-semibold text-white">{{ card.title }}</h2>
        <p class="text-sm text-yellow-100 mt-1">{{ card.subtitle }}</p>
      </a>
    {% endfor %}

  </div>
</section>


<section class="min-h-screen bg-black flex flex-col items-center justify-center px-8 py-20 font-sans text-yellow-400">
  <h1 class="text-5xl font-bold mb-16 tracking-wide text-yellow-300 drop-shadow-lg">Welcome to <span class="text-yellow-400">Bandito</span></h1>
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-12 max-w-7xl w-full">

    {% for card in cards %}
      <a href="{{ card.path }}"
         class="relative flex flex-col items-center rounded-3xl p-12 bg-black bg-opacity-30 backdrop-blur-lg ring-1 ring-yellow-600 hover:ring-yellow-400 shadow-lg transition duration-500 transform hover:scale-105">
        <div class="text-yellow-400 mb-6" aria-hidden="true">{{ card.icon|raw }}</div>
        <h2 class="text-3xl font-semibold mb-2 text-yellow-300">{{ card.title }}</h2>
        <p class="text-yellow-200 text-center">{{ card.subtitle }}</p>
        <span class="absolute bottom-6 right-6 text-yellow-500 text-3xl opacity-50 select-none pointer-events-none">→</span>
      </a>
    {% endfor %}

  </div>
</section>

<section class="min-h-screen bg-gray-50 flex flex-col items-center justify-center px-8 py-20 font-sans text-gray-800">
  <h1 class="text-5xl font-bold mb-16 tracking-tight text-yellow-600">Welcome to <span class="text-yellow-500">Bandito</span></h1>
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-12 max-w-7xl w-full">

    {% for card in cards %}
      <a href="{{ card.path }}"
         class="flex flex-col items-center rounded-xl bg-yellow-50 p-10 shadow-md hover:shadow-lg transition-shadow duration-300 transform hover:-translate-y-1">
        <div class="mb-5 text-yellow-400" aria-hidden="true">{{ card.icon|raw }}</div>
        <h2 class="text-2xl font-semibold mb-2">{{ card.title }}</h2>
        <p class="text-yellow-600 text-center">{{ card.subtitle }}</p>
      </a>
    {% endfor %}

  </div>
</section>

{% endblock %}
#}